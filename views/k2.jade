html
   head
      title Person
   body
      if(type == "error")
         h3(style = "color:red") #{message}
      else
         h3 New person, 
            name: #{JSON.stringify(person.name)}, 
            age: #{person.age} and 
            nationality: #{JSON.stringify(person.nationality)} added!